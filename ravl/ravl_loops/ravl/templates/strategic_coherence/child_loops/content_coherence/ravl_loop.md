# Coherence Loop

This loop is for analyzing internal consistency in content.

## Implementation

This loop **delegates** to the framework's `ravl.library.content_coherence` loop.

See `config/ravl.toml` for delegation configuration.
Run the loop with --show-config flag to see what configuration it will use