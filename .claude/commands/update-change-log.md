---
description: Look at git history and extract new items for the change log
---

## Context

People should be able to look at the change log and see what updates have been made to the content from the front end. They should not see git or deployment or background changes, only the semantically meaningful content updates.

## Your task

- Look at changelog.md, if it exists
- Look at the git commit history
- Identify all meaningful content updates that have been made since changelog.md was last updated
- Also check if any of the very recent updates should have updated text based on recent changes rather than a new section being added
- Collate a list of items, focussing on the problem statement/value/outcome not implementation details
- Confirm with the user that the list is correct by providing a bullet summary
- If they are happy then update the changelog.md, keeping the same structure and format that is there and putting newer items at the top.
