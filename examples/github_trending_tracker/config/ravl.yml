name: github_trending_tracker
description: Python RAVL loop that tracks GitHub trending repositories and learns trending patterns
type: example
loop_type: python

execution_timeout: 60

# Dependencies (automatically installed)
allowed_dependencies:
  requests:
    min_version: '2.31.0'
    max_version: '3.0.0'
