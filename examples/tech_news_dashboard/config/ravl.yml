description: Parent orchestrator that coordinates 3 news sources and generates meta-insights
type: example
loop_type: markdown

execution_timeout: 180

# Dependencies for aggregation and synthesis
allowed_dependencies:
  requests:
    min_version: '2.31.0'
    max_version: '3.0.0'
  feedparser:
    min_version: '6.0.0'
    max_version: '7.0.0'
  anthropic:
    min_version: '0.25.0'
    max_version: '1.0.0'
